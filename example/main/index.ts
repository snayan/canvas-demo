import Clock from '../clock';
import Sh from '../sh';
import Axes from '../axes';
import Cutout from '../cutout';

new Clock().render();
new Sh().render();
new Axes().render();
new Cutout().render();
