import Canvas from 'common/canvas';

export default class EaseOutCanvas extends Canvas {
  constructor() {
    super();
  }
  public render(container: HTMLElement) {
    super.render(container);
  }
}
